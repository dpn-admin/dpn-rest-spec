summary: Returns a list of bag details matching the uuid
description: Returns a list of bag details matching the uuid
tags:
  - Reporting/Member
parameters:
  - name: uuid
    in: path
    description: UUID of the target member
    required: true
    type: string
    format: uuid
  - name: bag_uuid
    in: path
    description: UUID of the target bag 
    required: true
    type: string
    format: uuid
responses:
  '200':
    description: Success
    schema:
      $ref: '#/definitions/BagDescriptive'
  '401': 
    $ref: '#/responses/401'
  '404': 
    $ref: '#/responses/404'

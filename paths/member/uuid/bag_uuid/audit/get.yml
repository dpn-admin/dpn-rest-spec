summary: List of audited events for the requested bag by uuid
description: List of audited events for the requested bag by uuid
tags:
  - Reporting/Memeber
parameters:
  - name: uuid
    in: path
    description: UUID of the target member
    required: true
    type: string
    format: uuid
  - name: bag_uuid
    in: path
    description: UUID of the target bag
    required: true
    type: string
    format: uuid
responses:
  '200':
    description: Success
    schema:
      $ref: '#/defintions/AuditResultList'
  '401':
    $ref: '#/responses/401'

summary: List of digests
description: >
  This endpoint is meant to facilitate synchronizing of data.  It
  returns a list of digests.
tags:
  - Digest
parameters:
  - $ref: '#/parameters/before_created'
  - $ref: '#/parameters/after_created'
  - $ref: '#/parameters/page'
  - $ref: '#/parameters/page_size'
  - $ref: '#/parameters/order_by_created_at'
responses:
  '200':
    description: Success
    schema:
      $ref: '#/definitions/DigestResultList'
  "401":
    $ref: '#/responses/401'

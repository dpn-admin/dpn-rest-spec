summary: List of restoration requests handled by a node namespace
description: List of restoration requests handled by a node namespace
tags:
  - Reporting/Node
parameters:
  - $ref: '#/parameters/namespace'
  - $ref: '#/parameters/after_created'
  - $ref: '#/parameters/before_created'
  # - $ref: '#/parameters/restore_status'
responses:
  '200':
    description: Success
    schema:
      $ref: '#/definitions/RestoreRequestResultList'

name: replicated_by
in: query
description: >
  The namespace of the replicating node on which 
  to filter.  Multiple filters can be used, separated
  by comma.  The result will include the union (OR) 
  of the filtered results.
required: false
type: string
